<h2><%= @flat.name %></h2>
<h5><%= @flat.description %></h5>
<p><%= @flat.address %></p>
<p>Per night, you will pay <%= @flat.price_per_night %> $</p>
<p>Maximum of guests allowed : <%= @flat.number_of_guests %></p>

<%= link_to "Back", flats_path, class: "btn btn-primary" %>

<%= link_to "Edit", edit_flat_path, class: "btn btn-warning" %>

<%= link_to "Delete",
  flat_path(@flat),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"},
  class: "btn btn-danger"%>
